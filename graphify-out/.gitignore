# Local-only graphify artifacts — do NOT commit (per graphify team-setup guidance).
# Shared & tracked: graph.json, graph.html, GRAPH_REPORT.md, .graphify_labels.json

# mtime-based; breaks after `git clone`, rebuilt locally
manifest.json
# local extraction cost ledger
cost.json
# per-machine AST cache; rebuilt for free by `graphify update .`
cache/
# absolute machine path, e.g. C:\exp\my-app
.graphify_root
